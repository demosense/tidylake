---
title: Installation
---

The package is currently distributed directly from GitHub (PyPI release pending):

```bash
uv add https://github.com/demosense/tidylake.git
```

!!! warning "No batteries included"
    `tidylake` does not install additional compute/data frameworks by default.
    If you want to run demos or scaffold projects for specific engines, install the corresponding framework dependencies separately.
